{
  "private": true,
  "name": "{{slugName}}",
  "version": "1.0.0",
  "scripts": {
    "lint": "{{lintScript}}",
    "test": "echo 'No test specified' && exit 0",
    "build": "echo 'No build specified' && exit 0",
    "deploy-env": "echo 'No deploy-env specified' && exit 0",
    "start": "echo 'No start specified' && exit 0"
  },
  "lint-staged": {
    "*.js": ["yarn lint --fix", "git add"]
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged",
      "pre-push": "yarn deploy-env"
    }
  },
  "author": "{{author}}"
}
